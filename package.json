{
  "name": "boom-meet",
  "version": "1.0.0",
  "description": "Video conferencing application built with React, Express, Socket.IO & PeerJS",
  "scripts": {
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-all": "cd server && npm install && cd ../client && npm install && cd ..",
    "build": "cd client && npm install --include=dev && npm run build",
    "start": "cd server && npm install --production && node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
